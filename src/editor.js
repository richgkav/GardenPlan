export class Editor {
    constructor() [

    ]
}

class EditorField {
    constructor(x, y, rotation, colour) {
        this.x = x;
        this.y = y
        this.rotation = rotation;
        this.colour = colour;
    }
}